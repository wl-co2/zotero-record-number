# Zotero Record Number 安装与使用

## 安装

1. 在 Zotero 中打开“工具 → 插件”（部分版本显示为“附加组件”）。
2. 点击齿轮菜单，选择“Install Add-on From File…”／“从文件安装插件”。
3. 选择 `dist\zotero-record-number-1.0.3.xpi`。
4. 如果 Zotero 要求重启，请重启。
5. 文献列表未自动显示时，在列表表头上右键并勾选 `Record Number`。

说明：Zotero 7.0.15 要求插件清单必须有 `update_url`。本插件将通过 `wl-co2/zotero-record-number` GitHub 仓库中的 `updates.json` 检查版本；仓库发布前，检查更新会返回文件不存在，但不影响手动安装和使用。

## 第一次使用

在负责导入新文献的主电脑上：

1. 打开“工具 → Record Number 编号”。
2. 勾选“在本机自动发号（仅主电脑启用）”。
3. 确认后，插件会按加入日期和条目 ID 的稳定顺序，为“我的文库”中缺失编号的普通文献发号。

首次批量发号时会显示已处理数量、总数和百分比；以后导入单篇文献时在后台静默自动发号。

其他电脑也可以安装插件以显示编号，但不要启用自动发号。已经写入 `Extra` 的编号会作为文献元数据由 Zotero 自身同步。

## 检查和手动补发

- “为缺失条目分配编号…”：只处理没有 `Record Number` 行的文献。
- 批量分配期间会显示编号进度，完成后仍会弹出处理结果。
- “检查编号…”：报告缺失、无效及重复编号；删除留下的空号不会被视为错误。

## 编号规则

- 所有分类共用一套编号，不按分类重新开始。
- 只处理“我的文库”，不处理群组文库、附件和笔记。
- 编号是纯数字，保存在 `Extra` 中，例如 `Record Number: 31`。
- 删除后不补号；修改题录、排序和切换分类不会改变编号。
- 无效或重复编号不会自动覆盖，请先通过“检查编号…”确认。

## 卸载

在 Zotero 的插件管理界面禁用或移除即可。卸载不会删除已经写入文献 `Extra` 的编号。
